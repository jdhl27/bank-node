/**
 * Create a new account
 */
function createAccount(newAccount) {
    if (!newAccount.holder) {
        return "El propietario es obligatorio"
    }
    
}

/**
 * Delete a account
 */
function deleteAccount() {
    
}

/**
 * Add funds to account
 */
function addFunds() {
    
}

module.exports = {
    createAccount,
    deleteAccount,
    addFunds
}